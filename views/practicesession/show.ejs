<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Practice Session Details</title>
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  
    <h1>Practice on <%= new Date(session.date).toDateString() %></h1>

  <ul>
    <li><strong>Stroke Type:</strong> <%= session.stroke_type %></li>
    <li><strong>Laps:</strong> <%= session.laps %></li>
    <li><strong>Distance:</strong> 
      <%= ((session.laps * 25) / 1760).toFixed(2) %> miles
    </li>
    <li><strong>Duration:</strong> <%= session.durationMinutes %> minutes</li>
    <li><strong>Notes:</strong> <%= session.notes || 'None' %></li>
  </ul>

  <p>
    <a href="/practicesession">⬅ Back to Practice Log</a>
  </p>

  <p>
    <a href="/practicesession/<%= session._id %>/edit">Edit This Session</a>
  </p>

  <form action="/practicesession/<%= session._id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this practice session?');">
  <button type="submit" style="background-color: #b22222; color: white; border: none; padding: 0.5rem 1rem; border-radius: 6px; font-weight: bold;">Delete This Session</button>
</form>
</body>
</html>